# Ansible-Edu
Ansible education education tasks:

1. Install Ansinle
2. Configure few VMs with different kinds of Linux distro and connect it through the Ansible
3. Provide simlpe install nginx playbook on VMs/ The playbook should manage nginx service using start, stop, enable and disable
4. Provide playbook install zip, unzip, gzip, nano (using Lists). And delete unzip, nano
 
