<h1>NGINX on {{ ansible_hostname }}</h1>

<pre>
Installed by Ansible
Server hostname is {{ ansible_fqdn }} and IP is {{ ansible_default_ipv4.address }}

{{ ansible_os_family }} / {{ ansible_distribution }} / {{ ansible_distribution_major_version }}
<pre>
